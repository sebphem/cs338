# Intro to backend

## Getting Started

Windows:

``` bash
python -m venv venv
venv/Scripts/activate
```

Mac/Linux:

```bash
python -m venv venv
source venv/bin/activate
python -m pip install -r requirements.txt
```
