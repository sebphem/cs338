import React from 'react';
import RedditProfilePage from './RedittRegistration.js';

export default function HomePage() {
    return (
        <div>
            <h1 className="text-center">Cupidity</h1>
            <p className="text-center">Where every swipe is right.</p>
            <RedditProfilePage/>
        </div>
    );
}
